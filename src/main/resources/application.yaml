spring:
  datasource:
    url: jdbc:mysql://localhost:3306/article?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: article_admin
    password: Dlehdgns0126!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true